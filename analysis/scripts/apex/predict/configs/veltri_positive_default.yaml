# APEX prediction config for mutants (veltri_positive) using default pathogen set
input_dir: results/mutants/veltri_positive
output_dir: results/apex_predictions/results/mutants/veltri_positive/default
file_glob: "*.csv"
sequence_column: mutant
use_tqdm: true
apex_predictor_kwargs:
  path: default
  device: cuda
  batch_size: 10000
