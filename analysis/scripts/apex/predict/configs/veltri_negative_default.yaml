# APEX prediction config for mutants (veltri_negative) using default pathogen set
input_dir: results/data/mutants/veltri_negative
output_dir: results/data/apex/veltri_negative/default
file_glob: "*.csv"
sequence_column: mutant
use_tqdm: true
apex_predictor_kwargs:
  path: default
  device: cuda
  batch_size: 10000
